# Demo app for testing json2Typescript

## After cloning:

`npm install`

## To test in browser:

`ionic serve`

## To build in debug mode:

- `ionic cordova build ios`
- _open project in XCode and run_

## To build in prod mode:

- `ionic cordova build ios --prod`
- _open project in XCode and run_
